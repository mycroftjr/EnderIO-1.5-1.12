repositories {
  maven {
    name 'DVS1 Maven FS'
    url 'https://dvs1.progwml6.com/files/maven/'
  }
}

dependencies {
  implementation project(":enderio-base")
  fg.deobf "slimeknights.mantle:Mantle:${mantle_version}"
  fg.deobf "slimeknights:TConstruct:${tic_version}"
}
